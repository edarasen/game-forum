json.status do
  json.code 200
  json.message "Moderator application now pending for approval."
end

json.data do
  json.extract! current_user, :id, :username, :email, :role

  json.created_at current_user.created_at
  json.updated_at current_user.updated_at
end

