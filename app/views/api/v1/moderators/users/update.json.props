json.status do
  json.code 200
  json.message "#{@user.username}'s credentials has been updated!"
end

json.data do
  json.extract! @user, :id, :username, :email, :role

  json.created_at @user.created_at
  json.updated_at @user.updated_at
end
