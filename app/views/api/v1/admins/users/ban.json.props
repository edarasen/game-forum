json.status do
  json.code 200
  json.message "#{@user.role} has been banned."
end

json.data do
  json.extract! @user, :id, :username, :email, :role, :deactivated, :deactivated_at

  json.created_at @user.created_at
  json.updated_at @user.updated_at
end
