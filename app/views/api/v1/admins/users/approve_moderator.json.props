json.status do
  json.code 200
  json.message "#{@user.username} is now a moderator."
end

json.data do
  json.extract! @user, :id, :username, :email, :role, :moderator_status, :mod_approval_date

  json.created_at @user.created_at
  json.updated_at @user.updated_at
end
