json.status do
  json.code 200
  json.message "User reactivated successfully"
end

json.data do
  json.extract! @user, :id, :username, :email, :role, :moderator_status, :deactivated, :deactivated_at, :created_at, :updated_at
end