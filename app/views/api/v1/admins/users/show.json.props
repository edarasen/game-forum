json.status do
  json.code 200
  json.message "You are now viewing #{@user.username}"
end

json.data do
  json.extract! @user, :id, :username, :email, :role, :deactivated

  json.created_at @user.created_at
  json.updated_at @user.updated_at
end
